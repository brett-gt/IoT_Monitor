import re

#--------------------------------------------------------------------------------
def parse(line):
    ''' Entry point into text parsing.  Distributes out parsing to various functions
        with the intent being each subfunction takes appropriate action based on text
        
        Argument:
            line - line of text to be parsed
    '''
    parseShout(line)
    
    return result;

#--------------------------------------------------------------------------------
def parseShout(line):
    ''' Parse shout to look for keywords
    '''
    print(line)


#--------------------------------------------------------------------------------
def parseLogin(line):
    if(line == "What is your character's name?")
        print("Login")

#--------------------------------------------------------------------------------






